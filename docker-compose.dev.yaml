services:
  postgres:
    ports:
      - "5432:5432"

  app:
    env_file:
      - .env.dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
